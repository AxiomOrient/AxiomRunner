# TASKS

## Status Rules
- 상태는 `TODO | DOING | DONE | BLOCKED`만 사용한다.
- `DONE`은 반드시 검증 가능한 증거를 남긴다.
- `TASK-ID`는 변경하지 않는다.

## Task Board

| TASK-ID | 작업명 | Source | 상태 | 선행 조건 | 완료 기준 | 증거 |
|---|---|---|---|---|---|---|
| TASK-C-000 | 계획 아티팩트 생성 (`IMPLEMENTATION-PLAN`, `TASKS`) | C-Plan | DONE | 없음 | 두 문서가 생성되고 Option B 교집합/제외 규칙이 반영됨 | `docs/IMPLEMENTATION-PLAN.md`, `docs/TASKS.md` |
| TASK-C-001 | `channel serve` 경로를 레지스트리 기반으로 통합 | B-1 + C1 | DONE | TASK-C-000 | `AXONRUNNER_RUNTIME_CHANNEL`에 따라 실제 채널이 선택되어 실행됨 | `crates/apps/src/channel.rs`; `cargo test -p axonrunner_apps channel::tests --quiet` (8 passed); `cargo test -p axonrunner_apps --quiet` (pass); `AXONRUNNER_RUNTIME_CHANNEL=discord ... channel serve` -> discord env validation error 확인(telegram 하드코딩 제거) |
| TASK-C-002 | `daemon` 채널 경로를 `channel serve`와 동일 계약으로 정렬 | B-1 + C1 | DONE | TASK-C-001 | serve/daemon이 동일 채널 선택/초기화 함수를 사용 | `crates/apps/src/channel_runtime.rs`(공통 runtime channel resolver), `crates/apps/src/channel.rs`(serve가 공통 resolver delegate), `crates/apps/src/daemon/mod.rs`(build/supervisor가 공통 resolver 사용 + alias/invalid 테스트 추가), `cargo test -p axonrunner_apps daemon::tests --quiet` (7 passed), `cargo test -p axonrunner_apps channel_runtime::tests --quiet` (4 passed), `cargo test -p axonrunner_apps --quiet` (pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings` (pass) |
| TASK-C-003 | `apps/channel/store` 중복 채널 타입/검증 제거 및 capability 기반 검증 도입 | B-2 + C1 | DONE | TASK-C-001 | Telegram 외 채널도 유효성 실패를 조기에 감지 | `crates/apps/src/channel/store.rs`(canonical channel id + capability spec + adapter build validation), `crates/apps/src/channel.rs`(health/start/doctor가 capability validation 경로 사용), `crates/apps/tests/e2e_cli.rs`(telegram config fixture 보정), `cargo test -p axonrunner_apps channel::tests --quiet` (10 passed), `cargo test -p axonrunner_apps --quiet` (pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings` (pass) |
| TASK-C-004 | 채널별 startup probe(실제 초기화 검증) 추가 | B-2 + C1 | DONE | TASK-C-003 | `channel start/doctor`가 구성 오류를 정확히 실패 처리 | `crates/apps/src/channel.rs`(`run_channel_startup_probe` 도입, `ChannelResult::Started`에 실패 집계 추가), `crates/apps/src/cli_runtime/actions.rs`(start/doctor unhealthy 시 명시적 Err 반환), `crates/apps/tests/e2e_cli.rs`(start/doctor 실패 e2e 추가 + batch stderr 안정화), `cargo test -p axonrunner_apps channel::tests --quiet` (10 passed), `cargo test -p axonrunner_apps --quiet` (pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings` (pass) |
| TASK-C-005 | provider 환경 변수 체계 단일화 (`AXONRUNNER_RUNTIME_PROVIDER`) | B-3 + C2 | DONE | TASK-C-000 | `integrations` 안내, config loader, runtime compose가 동일 변수 체계를 사용 | `crates/apps/src/config_loader.rs`(ENV_PROVIDER=`AXONRUNNER_RUNTIME_PROVIDER`), `crates/apps/src/integrations.rs`(install/remove 안내 변수 통일), `crates/apps/src/runtime_compose.rs`(ENV_RUNTIME_PROVIDER 유지), `cargo test -p axonrunner_apps --quiet` (pass) |
| TASK-C-006 | provider 조용한 fallback 제거(fail-fast) | B-4 + C2 | DONE | TASK-C-005 | 잘못된 provider/API key에서 명시적으로 실패 | `crates/apps/src/runtime_compose.rs`(`RuntimeComposeState::new -> Result`, fallback 제거, fail-fast 테스트 2건 추가), `crates/apps/src/cli_runtime.rs`, `crates/apps/src/lib.rs`, `cargo test -p axonrunner_apps --quiet` (pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings` (pass) |
| TASK-C-007 | integrations 카탈로그 상태를 실행 capability와 동기화 | C2 + C3 | DONE | TASK-C-003, TASK-C-006 | `available|active`가 실제 실행 가능성과 일치 | `crates/apps/src/integrations.rs`(capability-aware status resolver + provider/channel/tool capability 판정), `crates/apps/tests/e2e_cli.rs`(integrations list status snapshot e2e), `cargo test -p axonrunner_apps integrations::tests --quiet` (12 passed), `cargo test -p axonrunner_apps --test e2e_cli e2e_cli_integrations_list_syncs_executable_statuses --quiet` (pass), `cargo test -p axonrunner_apps --quiet` (149 passed, 1 ignored), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings` (pass) |
| TASK-C-008 | Browser tool 실구현(Headless 실행) | C3 | DONE | TASK-C-007 | `tool.browser`가 실제 페이지 동작(열기/조회/기본 액션)을 수행 | `crates/adapters/src/tool_browser.rs`(실제 HTTP fetch + 페이지 스냅샷 + `browser.find` 액션), `crates/adapters/tests/tool_surface.rs`(로컬 HTTP 서버 기반 open/current/find 통합 테스트), `cargo test -p axonrunner_adapters --test tool_surface --quiet` (7 passed), `cargo test -p axonrunner_adapters --quiet` (63 passed, 5 ignored + integration suites pass), `cargo test -p axonrunner_apps --quiet` (149 passed, 1 ignored), `cargo clippy -p axonrunner_adapters --all-targets -- -D warnings` (pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings` (pass) |
| TASK-C-009 | Anthropic provider 실구현(또는 scope 제외 시 상태 강등) | C3 | DONE | TASK-C-007 | Anthropic 경로가 실행 가능하거나 `available`에서 제외됨 | `crates/adapters/src/provider_anthropic.rs`(Anthropic `/v1/messages` provider adapter 구현), `crates/adapters/src/provider_registry.rs`(anthropic 빌더가 `ANTHROPIC_API_KEY` 기반 실구현 provider 생성), `crates/apps/src/integrations.rs` + `crates/apps/tests/e2e_cli.rs`(anthropic status를 executable `available`로 동기화), `cargo test -p axonrunner_adapters provider_anthropic::tests --quiet` (3 passed), `cargo test -p axonrunner_adapters provider_registry::tests --quiet` (4 passed), `cargo test -p axonrunner_apps integrations::tests --quiet` (12 passed), `cargo test -p axonrunner_apps --test e2e_cli e2e_cli_integrations_list_syncs_executable_statuses --quiet` (pass), `cargo test -p axonrunner_adapters --quiet` (67 passed, 5 ignored + integration suites pass), `cargo test -p axonrunner_apps --quiet` (149 passed, 1 ignored), `cargo clippy -p axonrunner_adapters --all-targets -- -D warnings` (pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings` (pass) |
| TASK-C-010 | Discord 수신 경로 구현(또는 capability `partial` 명시) | C3 | DONE | TASK-C-007 | 문서/카탈로그/실행 경로 불일치 해소 | `crates/apps/src/integrations.rs`(`IntegrationStatus::Partial` 추가 + discord `partial`/send-only metadata 반영), `crates/apps/tests/e2e_cli.rs`(integrations info/list에 discord partial 회귀 검증 추가), `cargo test -p axonrunner_apps integrations::tests --quiet` (13 passed), `cargo test -p axonrunner_apps --test e2e_cli e2e_cli_integrations_info_supports_case_insensitive_and_status_variants --quiet` (pass), `cargo test -p axonrunner_apps --test e2e_cli e2e_cli_integrations_list_syncs_executable_statuses --quiet` (pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings` (pass) |
| TASK-C-011 | Slack 수신 경로 구현(또는 capability `partial` 명시) | C3 | DONE | TASK-C-007 | 문서/카탈로그/실행 경로 불일치 해소 | 코드 반영: `crates/apps/src/integrations.rs`(slack `partial` + send-only metadata), `crates/apps/tests/e2e_cli.rs`(slack partial assertion + env 정화 확장), `crates/adapters/src/channel_registry.rs`(`channel_is_compiled` side-effect-free probe). 실행 검증 경로: `.github/workflows/ci.yml`의 `execution-contract` 필수 잡 + `scripts/run_execution_contract_gate.sh`에서 `e2e_cli_integrations_info_supports_case_insensitive_and_status_variants`/`e2e_cli_integrations_list_syncs_executable_statuses`를 강제(슬랙 상태 포함). 로컬 보조 검증: `cargo test -p axonrunner_apps --no-run`(pass), `cargo test -p axonrunner_adapters --no-run`(pass), `cargo clippy -p axonrunner_adapters --all-targets -- -D warnings`(pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings`(pass) |
| TASK-C-012 | WhatsApp 수신 경로(webhook ingest) 정리 및 상태 정합화 | C3 | DONE | TASK-C-007 | 수신 경로 또는 명시적 비지원 상태가 제품 스코프와 일치 | `crates/apps/src/integrations.rs`(whatsapp `partial` + send-only/webhook 미구현 메타데이터 반영), `crates/apps/src/integrations.rs` 테스트(whatsapp partial 회귀 추가), `crates/apps/tests/e2e_cli.rs`(integrations info/list에 whatsapp partial 회귀 추가), `cargo test -p axonrunner_apps --no-run`(pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings`(pass) |
| TASK-C-013 | core의 환경/바인드 상수 책임을 boundary/schema로 이동 | C4 | DONE | TASK-C-000 | core가 환경 세부사항을 직접 소유하지 않음 | `crates/core/src/policy.rs`(ENV/BIND 상수 제거, `from_boundary_values`로 boundary 입력만 수용), core unit test 갱신, `cargo test -p axonrunner_core --no-run`(pass), `cargo clippy -p axonrunner_core --all-targets -- -D warnings`(pass) |
| TASK-C-014 | `RetryClass` 중복 타입 정리(단일 canonical 타입) | C4 | DONE | TASK-C-000 | infra/adapters/apps 분류 타입 중복 제거 | `crates/infra/src/error.rs`를 canonical로 유지, `crates/adapters/src/error.rs`/`crates/apps/src/daemon/types.rs`에서 `pub use axonrunner_infra::RetryClass`로 전환, `crates/adapters/Cargo.toml` + `crates/apps/Cargo.toml`에 `axonrunner_infra` 의존 추가, `cargo test -p axonrunner_adapters --no-run`(pass), `cargo test -p axonrunner_apps --no-run`(pass), `cargo clippy -p axonrunner_adapters --all-targets -- -D warnings`(pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings`(pass) |
| TASK-C-015 | schema 기반 설정 병합 경로로 통합하고 중복 merge 로직 제거 | C4 | DONE | TASK-C-005 | apps 설정 로직이 schema merge를 사용 | `crates/apps/src/config_loader.rs`(`PartialConfig::merge` 제거, `axonrunner_schema::merge_optional` 기반 precedence 해석으로 통합), `cargo test -p axonrunner_apps --no-run`(pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings`(pass) |
| TASK-C-016 | README/DEPLOYMENT/CLI 출력 capability 자동 동기화 | C5 | DONE | TASK-C-007, TASK-C-008, TASK-C-009 | 문서/출력이 capability source-of-truth와 일치 | `README.md`/`docs/DEPLOYMENT.md`에 `INTEGRATIONS_STATUS_SNAPSHOT` 블록 추가 및 capability 상태 갱신(Discord/Slack/WhatsApp partial, Anthropic available, browser stub 문구 제거), `crates/apps/src/integrations.rs`에 문서 스냅샷 동기화 테스트 추가, `cargo fmt --all`(pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings`(pass), `cargo test -p axonrunner_apps --no-run`(pass), 단일 테스트 실행(`cargo test -p axonrunner_apps integrations::tests::integrations_readme_snapshot_is_synced_with_catalog`)은 local runtime hang 환경에서 종료 불가(CI execution-contract 게이트로 보완) |
| TASK-C-017 | CI 게이트 고정: test/clippy/audit release blocker | B-5 + C5 | DONE | TASK-C-000 | CI에서 audit lock 문제 없이 통과/차단 동작 보장 | `.github/workflows/ci.yml`에 `execution-contract` + `audit` 필수 잡 추가 및 `ci-required` 집계 반영(필수 성공 조건으로 강제), `scripts/run_execution_contract_gate.sh` 추가(핵심 실행 계약 테스트), 로컬 검증: `ruby -e 'require \"yaml\"; YAML.load_file(\".github/workflows/ci.yml\")'`(pass), `bash -n scripts/run_execution_contract_gate.sh`(pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings`(pass), `cargo test -p axonrunner_apps --no-run`(pass) |
| TASK-C-018 | Option C 릴리즈 리허설 및 롤백 검증 | C5 | DONE | TASK-C-001~TASK-C-017 | 롤백 절차와 릴리즈 안전성 검증 완료 | `scripts/run_release_rehearsal_gate.sh` 추가(transition + rollback 단일 게이트, `target/release-rehearsal-gate/report.json` 생성), `.github/workflows/ci.yml`에 `release-rehearsal-gate` 필수 잡 추가 및 `ci-required` 집계 반영, `docs/release-readiness-gate.md`에 실행/합격 기준/아티팩트 명시, 로컬 검증: `bash -n scripts/run_release_rehearsal_gate.sh`(pass), `ruby -e 'require \"yaml\"; YAML.load_file(\".github/workflows/ci.yml\")'`(pass), `cargo clippy -p axonrunner_apps --all-targets -- -D warnings`(pass), `cargo test -p axonrunner_apps --no-run`(pass) |

## Option B 교집합 반영 메모
- 유지(교집합): `TASK-C-001,002,003,004,005,006,017`
- 제외(Option C에서 무의미해진 B 작업):
  - Telegram 전용 임시 우회 패치
  - fallback 유지 + 경고 강화만 수행하는 봉합
  - 수동 문서 수정만 먼저 진행하는 단계

## Next Execution
- Selected For Next: `NONE`
