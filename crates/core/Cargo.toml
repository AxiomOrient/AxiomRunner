[package]
name = "axiomrunner_core"
version.workspace = true
edition.workspace = true
description = "AxiomRunner core contracts"

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
